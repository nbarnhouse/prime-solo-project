import React from 'react';
import { useHistory } from 'react-router-dom';

export default function AbsenceTeacher() {
  const history = useHistory();

  return <div className="container">Absence for Teachers</div>;
}
