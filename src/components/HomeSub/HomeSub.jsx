import React from 'react';
import { useHistory } from 'react-router-dom';

export default function HomeSub() {
  const history = useHistory();

  return <div className="container">Home for Subs</div>;
}
