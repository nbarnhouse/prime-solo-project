import React from 'react';
import { useHistory } from 'react-router-dom';

export default function HomeTeacher() {
  const history = useHistory();

  return <div className="container">Home for Teachers</div>;
}
