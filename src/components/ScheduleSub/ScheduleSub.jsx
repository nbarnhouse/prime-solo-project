import React from 'react';
import { useHistory } from 'react-router-dom';

export default function ScheduleSub() {
  const history = useHistory();

  return <div className="container">Schedule for Subs</div>;
}
